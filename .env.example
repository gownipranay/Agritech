# Copy to .env and fill in. Never commit the real .env file.

# Phase 4/5 — LLM advisory layer (summarization/translation only, see README)
ANTHROPIC_API_KEY=

# Phase 2 — weather integration
OPENWEATHER_API_KEY=

# Optional override; defaults to ml/checkpoints/disease_model.pt
DISEASE_MODEL_CHECKPOINT=
